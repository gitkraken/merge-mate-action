name: "Merge Mate: Review"
description: Apply or undo resolved changes on PR branches
author: GitKraken

branding:
  icon: git-merge
  color: blue

inputs:
  github-token:
    description: GitHub token for authentication
    required: false
  fork-push-token:
    description: Explicit token used only to probe/push fork branches when the primary token cannot access the fork
    required: false
  pr-number:
    description: PR number to process (required for workflow_dispatch)
    required: false
  action:
    description: "'apply' or 'undo' (required for workflow_dispatch)"
    required: false
  telemetry:
    description: Enable telemetry and error tracking
    required: false
    default: "true"
  diff-viewer-base-url:
    description: Base URL for the diff viewer
    required: false
    default: "https://gitkraken.dev"
  trigger-mode:
    description: "Trigger source: viewer | workflow_dispatch"

    required: false
    default: "workflow_dispatch"
  log-level:
    description: "error | warn | info | debug"
    required: false
    default: "info"

runs:
  using: node24
  main: ../dist/review/index.js
